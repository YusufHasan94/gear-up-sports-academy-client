
const SectionTitle = ({heading}) => {
    return (
        <div className="text-center ">
            <p className="md:text-4xl text-2xl font-semibold uppercase">{heading}</p>
        </div>
    );
};

export default SectionTitle;